# Core data / numerics
pandas>=2.2
numpy>=1.26
scipy>=1.13
pyarrow>=16.0

# Fetching
requests>=2.32
tqdm>=4.66

# Modeling
scikit-learn>=1.5
xgboost>=2.1
lightgbm>=4.4

# Uncertainty (valid methods — NOT input-noise bootstrap)
mapie>=0.9          # conformal prediction intervals with coverage guarantees

# Interpretability
shap>=0.46

# Survival (lifespan T50 / KM)
lifelines>=0.29

# Stats / plotting
statsmodels>=0.14
matplotlib>=3.9
seaborn>=0.13

# GSEA (cross-validated against a local implementation, Findings 15/20) + R-data reading
gseapy>=1.3
pyreadr>=0.5

# Synapse downloads (BCM-DMAS, EmoryDrosophilaTau) — requires the USER's own
# Synapse account/token, run in their own terminal; never handled by this env
synapseclient>=4.0

# Web app
streamlit>=1.37
plotly>=5.0
bed-reader>=1.0   # PLINK genotype reader

# Dev
pytest>=8.0
